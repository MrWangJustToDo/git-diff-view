import type { DiffFile } from "@git-diff-view/core";

export const DiffUnifiedView = (props: { diffFile: DiffFile }) => {
  return (
    <div>
      <h1>Unified Diff View</h1>
      <p>This is a placeholder for the unified diff view component.</p>
    </div>
  );
};
